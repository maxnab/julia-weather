import type { ICameraData } from './iCameraData';

interface IPlayer {
  day: ICameraData;
  lifetime: ICameraData;
  live: ICameraData;
  month: ICameraData;
  year: ICameraData;
}

export type { IPlayer };
